import styleClass from "./Footer.module.scss";

const Footer = (props) => {
    return (
        <div className={styleClass.footerContainer}>
            
        </div>
    )
}

export default Footer